# Pricing Catalog

Your pricing catalog describes the products and services you sell.

## Catalog Item Object

A catalog item represents a product or service that you sell. Catalog items can be used to generate [line items](#line-item-object) and can also be used by [plans](#plan-object).

### Attributes

```shell
<%= json data.pricing.catalog_items.object %>
```

```ruby
#<Invoiced::CatalogItem:0x3fdbf95e4d08 id=delivery> JSON: <%= json data.pricing.catalog_items.object %>
```

```php
Invoiced\CatalogItem JSON: <%= json data.pricing.catalog_items.object %>
```

```python
<CatalogItem id=delivery at 0x3fdbf95e4d08> JSON: <%= json data.pricing.catalog_items.object %>
```

```java
com.invoiced.entity.CatalogItem@192cafae JSON: <%= json data.pricing.catalog_items.object %>
```

Parameter | Type | Description
--------- | ---- | -----------
**id** | *integer* | The catalog item's unique ID
**object** | *string* | Object type, `catalog_item`
**name** | *string* | Catalog item name
**currency** | *string* | [3-letter ISO code](https://en.wikipedia.org/wiki/ISO_4217)
**unit_cost** | *number* | Unit cost or rate
**description** | *string* | Optional description
**type** | *string* | Optional line item type. Used to group line items by type in reporting
**taxable** | *boolean* | Excludes amount from taxes when `false`
**taxes** | *array* | Collection of Tax Rate Objects
**avalara_tax_code** | *string* | [Avalara-specific tax code](https://taxcode.avatax.avalara.com/)
**gl_account** | *string* | General ledger account code
**discountable** | *boolean* | Excludes amount from discounts when `false`
**created_at** | *timestamp* | Timestamp when created
**metadata** | *object* | A hash of key/value pairs that can store additional information about this object.

## Create a catalog item

```shell
curl "https://api.invoiced.com/catalog_items" \
  -u {API_KEY}: \
  -d id="delivery" \
  -d name="Delivery" \
  -d type="service" \
  -d unit_cost=100
```

```ruby
invoiced.CatalogItem.create(
  :id => "delivery",
  :name => "Delivery",
  :type => "service",
  :unit_cost => 100
)
```

```php
<?php

$invoiced->CatalogItem->create([
  'id' => "delivery",
  'name' => "Delivery",
  'type' => "service",
  'unit_cost' => 100
]);
```

```python
client.CatalogItem.create(
  id="delivery",
  name="Delivery",
  type="service",
  unit_cost=100
)
```

```java
CatalogItem catalogItem = invoiced.newCatalogItem();
catalogItem.id = "delivery";
catalogItem.name = "Delivery";
catalogItem.type = "service";
catalogItem.unit_cost = 100;
catalogItem.create();
```

> The above command returns JSON structured like this:

```shell
<%= json data.pricing.catalog_items.object %>
```

```ruby
#<Invoiced::CatalogItem:0x3fdbf95e4d08 id=15444> JSON: <%= json data.pricing.catalog_items.object %>
```

```php
Invoiced\CatalogItem JSON: <%= json data.pricing.catalog_items.object %>
```

```python
<CatalogItem id=15444 at 0x3fdbf95e4d08> JSON: <%= json data.pricing.catalog_items.object %>
```

```java
com.invoiced.entity.CatalogItem@cb8fd57 JSON: <%= json data.pricing.catalog_items.object %>
```

Create a new catalog item with this endpoint.

### HTTP Request

`POST /catalog_items`

### Attributes

Parameter | Type | Description
--------- | ---- | -----------
**id** | *integer* | The catalog item's unique ID
**name** | *string* | Catalog item name
**currency** | *string* | [3-letter ISO code](https://en.wikipedia.org/wiki/ISO_4217)
**unit_cost** | *number* | Unit cost or rate
**description** | *string* | Optional description
**type** | *string* | Optional line item type. Used to group line items by type in reporting
**taxable** | *boolean* | Excludes amount from taxes when `false`
**avalara_tax_code** | *string* | [Avalara-specific tax code](https://taxcode.avatax.avalara.com/)
**gl_account** | *string* | General ledger account code
**discountable** | *boolean* | Excludes amount from discounts when `false`
**metadata** | *object* | A hash of key/value pairs that can store additional information about this object.

## Retrieve a catalog item

```shell
curl "https://api.invoiced.com/catalog_items/:id" \
  -u {API_KEY}:
```

```ruby
catalogItem = invoiced.CatalogItem.retrieve("{CITEM_ID}")
```

```php
<?php

$catalogItem = $invoiced->CatalogItem->retrieve("{CITEM_ID}");
```

```python
catalogItem = client.CatalogItem.retrieve("{CITEM_ID}")
```

```java
CatalogItem catalogItem = invoiced.newCatalogItem().retrieve({CITEM_ID});

```

> The above command returns JSON structured like this:

```shell
<%= json data.pricing.catalog_items.object %>
```

```ruby
#<Invoiced::CatalogItem:0x3fdbf95e4d08 id=15444> JSON: <%= json data.pricing.catalog_items.object %>
```

```php
Invoiced\CatalogItem JSON: <%= json data.pricing.catalog_items.object %>
```

```python
<CatalogItem id=15444 at 0x3fdbf95e4d08> JSON: <%= json data.pricing.catalog_items.object %>
```

```java
com.invoiced.entity.CatalogItem@cb8fd57 JSON: <%= json data.pricing.catalog_items.object %>
```

This endpoint retrieves a specific catalog item.

### HTTP Request

`GET /catalog_items/:id`

## Update a catalog item

```shell
curl "https://api.invoiced.com/catalog_items/:id" \
  -u {API_KEY}: \
  -X PATCH \
  -d type="product"
```

```ruby
catalogItem.type = "product"
catalogItem.save
```

```php
<?php

$catalogItem->type = "product";
$catalogItem->save();
```

```python
catalogItem.type = "product"
catalogItem.save()
```

```java
catalogItem.type = "product";
catalogItem.save();
```

> The above command returns JSON structured like this:

```shell
<%= json data.pricing.catalog_items._update %>
```

```ruby
#<Invoiced::CatalogItem:0x3fdbf95e4d08 id=15444> JSON: <%= json data.pricing.catalog_items._update %>
```

```php
Invoiced\CatalogItem JSON: <%= json data.pricing.catalog_items._update %>
```

```python
<CatalogItem id=15444 at 0x3fdbf95e4d08> JSON: <%= json data.pricing.catalog_items._update %>
```

```java
com.invoiced.entity.CatalogItem@cb8fd57 JSON: <%= json data.pricing.catalog_items._update %>
```

Use this endpoint to update a catalog item.

### HTTP Request

`PATCH /catalog_items/:id`

### Attributes

Parameter | Type | Description
--------- | ---- | -----------
**name** | *string* | Catalog item name
**description** | *string* | Optional description
**type** | *string* | Optional line item type. Used to group line items by type in reporting
**metadata** | *object* | A hash of key/value pairs that can store additional information about this object.

## Delete a catalog item

```shell
curl "https://api.invoiced.com/catalog_items/:id" \
  -u {API_KEY}: \
  -X DELETE
```

```ruby
catalogItem.delete
```

```php
<?php

$catalogItem->delete();
```

```python
catalogItem.delete()
```

```java
catalogItem.delete();
```

> The above command returns `204 No Content`

This endpoint deletes a specific catalog item.

### HTTP Request

`DELETE /catalog_items/:id`

## List all catalog items

```shell
curl "https://api.invoiced.com/catalog_items" \
  -u {API_KEY}:
```

```ruby
catalog_items, metadata = invoiced.CatalogItem.list(:per_page => 3)
```

```php
<?php

list($catalogItems, $metadata) = $invoiced->CatalogItem->all(['per_page' => 3]);
```

```python
catalog_items, metadata = invoiced.CatalogItem.list(per_page=3)
```

```java
EntityList<CatalogItem> catalogItems = conn.newCatalogItem().listAll();
```

> The above command returns JSON structured like this:

```shell
[
  <%= json data.pricing.catalog_items.object, 1 %>
]
```

```ruby
[
  #<Invoiced::CatalogItem:0x3fdbf95e4d08 id=46225> JSON: <%= json data.pricing.catalog_items.object, 1 %>
]
```

```php
[
  Invoiced\CatalogItem JSON: <%= json data.pricing.catalog_items.object, 1 %>
]
```

```python
[
  <CatalogItem id=46225 at 0x3fdbf95e4d08> JSON: <%= json data.pricing.catalog_items.object, 1 %>
]
```

```java
[
  com.invoiced.entity.CatalogItem@c509a57 JSON: <%= json data.pricing.catalog_items.object, 1 %>
]
```

This endpoint retrieves all catalog items.

### HTTP Request

`GET /catalog_items`

### Query Parameters

Parameter | Description
--------- | -----------
**sort** *string* | Column to sort by, i.e. `name asc`
**filter** *object* | Filter object
**updated_after** *timestamp* | Only gets records updated after the given timestamp

## Plan Object

A plan describes a fixed amount that is billed to customers over a recurring interval. Used with [subscriptions](#subscription-object).

### Attributes

```shell
<%= json data.pricing.plans.object %>
```

```ruby
#<Invoiced::Plan:0x3fdbf95e4d08 id=starter> JSON: <%= json data.pricing.plans.object %>
```

```php
Invoiced\Plan JSON: <%= json data.pricing.plans.object %>
```

```python
<Plan id=starter at 0x3fdbf95e4d08> JSON: <%= json data.pricing.plans.object %>
```

```java
com.invoiced.entity.Plan@192cafae JSON: <%= json data.pricing.plans.object %>
```

Parameter | Type | Description
--------- | ---- | -----------
**id** | *integer* | The plan's unique ID
**object** | *string* | Object type, `plan`
**catalog_item** | *string* | Catalog item ID the plan belongs to
**name** | *string* | Plan name
**currency** | *string* | [3-letter ISO code](https://en.wikipedia.org/wiki/ISO_4217)
**amount** | *number* | Plan amount
**pricing_mode** | *string* | `per_unit`, `volume` or `tiered`
**quantity_type** | *string* | `constant` or `usage`
**interval** | *string* | One of `day`, `week`, `month`, `year`. The frequency with which a subscription should be billed.
**interval_count** | *number* | The number of intervals between each subscription billing. Defaults to `1`.
**tiers** | *array* | Example: `[{“max_qty”:50,”unit_cost”:100},{“min_qty”:51,”max_qty”:100,”unit_cost”:80},{“min_qty”:101,”unit_cost”:70}]`
**created_at** | *timestamp* | Timestamp when created
**metadata** | *object* | A hash of key/value pairs that can store additional information about this object.

## Create a plan

```shell
curl "https://api.invoiced.com/plans" \
  -u {API_KEY}: \
  -d id="starter" \
  -d name="Starter" \
  -d catalog_item="software-subscription" \
  -d amount=49 \
  -d interval="month" \
  -d interval_count=1 \
  -d pricing_mode="volume"
```

```ruby
invoiced.Plan.create(
  :id => "starter",
  :name => "Starter",
  :catalog_item => "software-subscription",
  :amount => 49,
  :interval => "month",
  :interval_count => 1,
  :pricing_mode => "volume"
)
```

```php
<?php

$invoiced->Plan->create([
  'id' => "starter",
  'name' => "Starter",
  'catalog_item' => "software-subscription",
  'amount' => 49,
  'interval' => "month",
  'interval_count' => 1,
  'pricing_mode' => "volume"
]);
```

```python
client.Plan.create(
  id="starter",
  name="Starter",
  catalog_item="software-subscription",
  amount=49,
  interval="month",
  interval_count=1,
  pricing_mode="volume"
)
```

```java
Plan plan = invoiced.newPlan();
plan.id = "starter";
plan.name = "Starter";
plan.catalog_item = "software-subscription";
plan.amount = 49;
plan.interval = "month";
plan.interval_count = 1;
plan.pricing_mode = "volume";
plan.create();
```

> The above command returns JSON structured like this:

```shell
<%= json data.pricing.plans.object %>
```

```ruby
#<Invoiced::Plan:0x3fdbf95e4d08 id=15444> JSON: <%= json data.pricing.plans.object %>
```

```php
Invoiced\Plan JSON: <%= json data.pricing.plans.object %>
```

```python
<Plan id=15444 at 0x3fdbf95e4d08> JSON: <%= json data.pricing.plans.object %>
```

```java
com.invoiced.entity.Plan@cb8fd57 JSON: <%= json data.pricing.plans.object %>
```

Create a new plan with this endpoint.

### HTTP Request

`POST /plans`

### Attributes

Parameter | Type | Description
--------- | ---- | -----------
**id** | *integer* | The plan's unique ID
**catalog_item** | *string* | Catalog item ID the plan belongs to
**name** | *string* | Plan name
**currency** | *string* | [3-letter ISO code](https://en.wikipedia.org/wiki/ISO_4217)
**amount** | *number* | Plan amount
**pricing_mode** | *string* | `per_unit`, `volume` or `tiered`
**quantity_type** | *string* | `constant` or `usage`
**interval** | *string* | One of `day`, `week`, `month`, `year`. The frequency with which a subscription should be billed.
**interval_count** | *number* | The number of intervals between each subscription billing. Defaults to `1`.
**tiers** | *array* | Example: `[{“max_qty”:50,”unit_cost”:100},{“min_qty”:51,”max_qty”:100,”unit_cost”:80},{“min_qty”:101,”unit_cost”:70}]`
**metadata** | *object* | A hash of key/value pairs that can store additional information about this object.

## Retrieve a plan

```shell
curl "https://api.invoiced.com/plans/:id" \
  -u {API_KEY}:
```

```ruby
plan = invoiced.Plan.retrieve("{PLAN_ID}")
```

```php
<?php

$plan = $invoiced->Plan->retrieve("{PLAN_ID}");
```

```python
plan = client.Plan.retrieve("{PLAN_ID}")
```

```java
Plan plan = invoiced.newPlan().retrieve({PLAN_ID});

```

> The above command returns JSON structured like this:

```shell
<%= json data.pricing.plans.object %>
```

```ruby
#<Invoiced::Plan:0x3fdbf95e4d08 id=15444> JSON: <%= json data.pricing.plans.object %>
```

```php
Invoiced\Plan JSON: <%= json data.pricing.plans.object %>
```

```python
<Plan id=15444 at 0x3fdbf95e4d08> JSON: <%= json data.pricing.plans.object %>
```

```java
com.invoiced.entity.Plan@cb8fd57 JSON: <%= json data.pricing.plans.object %>
```

This endpoint retrieves a specific plan.

### HTTP Request

`GET /plans/:id`

## Update a plan

```shell
curl "https://api.invoiced.com/plans/:id" \
  -u {API_KEY}: \
  -X PATCH \
  -d name="Standard"
```

```ruby
plan.name = "Standard"
plan.save
```

```php
<?php

$plan->name = "Standard";
$plan->save();
```

```python
plan.name = "Standard"
plan.save()
```

```java
plan.name = "Standard";
plan.save();
```

> The above command returns JSON structured like this:

```shell
<%= json data.pricing.plans._update %>
```

```ruby
#<Invoiced::Plan:0x3fdbf95e4d08 id=15444> JSON: <%= json data.pricing.plans._update %>
```

```php
Invoiced\Plan JSON: <%= json data.pricing.plans._update %>
```

```python
<Plan id=15444 at 0x3fdbf95e4d08> JSON: <%= json data.pricing.plans._update %>
```

```java
com.invoiced.entity.Plan@cb8fd57 JSON: <%= json data.pricing.plans._update %>
```

Use this endpoint to update a plan.

### HTTP Request

`PATCH /plans/:id`

### Attributes

Parameter | Type | Description
--------- | ---- | -----------
**name** | *string* | Plan name
**metadata** | *object* | A hash of key/value pairs that can store additional information about this object.

## Delete a plan

```shell
curl "https://api.invoiced.com/plans/:id" \
  -u {API_KEY}: \
  -X DELETE
```

```ruby
plan.delete
```

```php
<?php

$plan->delete();
```

```python
plan.delete()
```

```java
plan.delete();
```

> The above command returns `204 No Content`

This endpoint deletes a specific plan.

### HTTP Request

`DELETE /plans/:id`

## List all plans

```shell
curl "https://api.invoiced.com/plans" \
  -u {API_KEY}:
```

```ruby
catalog_items, metadata = invoiced.Plan.list(:per_page => 3)
```

```php
<?php

list($plans, $metadata) = $invoiced->Plan->all(['per_page' => 3]);
```

```python
plans, metadata = invoiced.Plan.list(per_page=3)
```

```java
EntityList<Plan> plans = conn.newPlan().listAll();
```

> The above command returns JSON structured like this:

```shell
[
  <%= json data.pricing.plans.object, 1 %>
]
```

```ruby
[
  #<Invoiced::Plan:0x3fdbf95e4d08 id=46225> JSON: <%= json data.pricing.plans.object, 1 %>
]
```

```php
[
  Invoiced\Plan JSON: <%= json data.pricing.plans.object, 1 %>
]
```

```python
[
  <Plan id=46225 at 0x3fdbf95e4d08> JSON: <%= json data.pricing.plans.object, 1 %>
]
```

```java
[
  com.invoiced.entity.Plan@c509a57 JSON: <%= json data.pricing.plans.object, 1 %>
]
```

This endpoint retrieves all plans.

### HTTP Request

`GET /plans`

### Query Parameters

Parameter | Description
--------- | -----------
**sort** *string* | Column to sort by, i.e. `name asc`
**filter** *object* | Filter object
**updated_after** *timestamp* | Only gets records updated after the given timestamp

## Tax Rate Object

A tax rate describes a tax that customers should be charged. Tax rates can be applied to [invoices](#invoice-object), [line items](#line-item-object), and [subscriptions](#subscription-object).

### Attributes

```shell
<%= json data.pricing.tax_rates.object %>
```

```ruby
#<Invoiced::TaxRate:0x3fdbf95e4d08 id=vat> JSON: <%= json data.pricing.tax_rates.object %>
```

```php
Invoiced\TaxRate JSON: <%= json data.pricing.tax_rates.object %>
```

```python
<TaxRate id=vat at 0x3fdbf95e4d08> JSON: <%= json data.pricing.tax_rates.object %>
```

```java
com.invoiced.entity.TaxRate@192cafae JSON: <%= json data.pricing.tax_rates.object %>
```

Parameter | Type | Description
--------- | ---- | -----------
**id** | *integer* | The tax rate's unique ID
**object** | *string* | Object type, `tax_rate`
**name** | *string* | Tax rate name
**currency** | *string* | [3-letter ISO code](https://en.wikipedia.org/wiki/ISO_4217)
**value** | *number* | Amount
**inclusive** | *boolean* | When true, pricing is calculated as tax-inclusive
**is_percent** | *boolean* | When true the value is a %
**created_at** | *timestamp* | Timestamp when created
**metadata** | *object* | A hash of key/value pairs that can store additional information about this object.

## Create a tax rate

```shell
curl "https://api.invoiced.com/tax_rates" \
  -u {API_KEY}: \
  -d id="vat" \
  -d name="VAT" \
  -d value=5 \
```

```ruby
invoiced.TaxRate.create(
  :id => "vat",
  :name => "VAT",
  :value => 5
)
```

```php
<?php

$invoiced->TaxRate->create([
  'id' => "vat",
  'name' => "VAT",
  'value' => 5
]);
```

```python
client.TaxRate.create(
  id="vat",
  name="VAT",
  value=5,
)
```

```java
TaxRate tax_rate = invoiced.newTaxRate();
tax_rate.id = "vat";
tax_rate.name = "VAT";
tax_rate.value = 5;
tax_rate.create();
```

> The above command returns JSON structured like this:

```shell
<%= json data.pricing.tax_rates.object %>
```

```ruby
#<Invoiced::TaxRate:0x3fdbf95e4d08 id=15444> JSON: <%= json data.pricing.tax_rates.object %>
```

```php
Invoiced\TaxRate JSON: <%= json data.pricing.tax_rates.object %>
```

```python
<TaxRate id=15444 at 0x3fdbf95e4d08> JSON: <%= json data.pricing.tax_rates.object %>
```

```java
com.invoiced.entity.TaxRate@cb8fd57 JSON: <%= json data.pricing.tax_rates.object %>
```

Create a new tax rate with this endpoint.

### HTTP Request

`POST /tax_rates`

### Attributes

Parameter | Type | Description
--------- | ---- | -----------
**id** | *integer* | The tax rate's unique ID
**name** | *string* | Tax rate name
**currency** | *string* | [3-letter ISO code](https://en.wikipedia.org/wiki/ISO_4217)
**value** | *number* | Amount
**inclusive** | *boolean* | When true, pricing is calculated as tax-inclusive
**is_percent** | *boolean* | When true the value is a %
**metadata** | *object* | A hash of key/value pairs that can store additional information about this object.

## Retrieve a tax rate

```shell
curl "https://api.invoiced.com/tax_rates/:id" \
  -u {API_KEY}:
```

```ruby
tax_rate = invoiced.TaxRate.retrieve("{TXRAT_ID}")
```

```php
<?php

$taxRate = $invoiced->TaxRate->retrieve("{TXRAT_ID}");
```

```python
tax_rate = client.TaxRate.retrieve("{TXRAT_ID}")
```

```java
TaxRate taxRate = invoiced.newTaxRate().retrieve({TXRAT_ID});

```

> The above command returns JSON structured like this:

```shell
<%= json data.pricing.tax_rates.object %>
```

```ruby
#<Invoiced::TaxRate:0x3fdbf95e4d08 id=15444> JSON: <%= json data.pricing.tax_rates.object %>
```

```php
Invoiced\TaxRate JSON: <%= json data.pricing.tax_rates.object %>
```

```python
<TaxRate id=15444 at 0x3fdbf95e4d08> JSON: <%= json data.pricing.tax_rates.object %>
```

```java
com.invoiced.entity.TaxRate@cb8fd57 JSON: <%= json data.pricing.tax_rates.object %>
```

This endpoint retrieves a specific tax rate.

### HTTP Request

`GET /tax_rates/:id`

## Update a tax rate

```shell
curl "https://api.invoiced.com/tax_rates/:id" \
  -u {API_KEY}: \
  -X PATCH \
  -d name="GST"
```

```ruby
tax_rate.name = "GST"
tax_rate.save
```

```php
<?php

$taxRate->name = "GST";
$taxRate->save();
```

```python
tax_rate.name = "GST"
tax_rate.save()
```

```java
taxRate.name = "GST";
taxRate.save();
```

> The above command returns JSON structured like this:

```shell
<%= json data.pricing.tax_rates._update %>
```

```ruby
#<Invoiced::TaxRate:0x3fdbf95e4d08 id=15444> JSON: <%= json data.pricing.tax_rates._update %>
```

```php
Invoiced\TaxRate JSON: <%= json data.pricing.tax_rates._update %>
```

```python
<TaxRate id=15444 at 0x3fdbf95e4d08> JSON: <%= json data.pricing.tax_rates._update %>
```

```java
com.invoiced.entity.TaxRate@cb8fd57 JSON: <%= json data.pricing.tax_rates._update %>
```

Use this endpoint to update a tax rate.

### HTTP Request

`PATCH /tax_rates/:id`

### Attributes

Parameter | Type | Description
--------- | ---- | -----------
**name** | *string* | Tax rate name
**metadata** | *object* | A hash of key/value pairs that can store additional information about this object.

## Delete a tax rate

```shell
curl "https://api.invoiced.com/tax_rates/:id" \
  -u {API_KEY}: \
  -X DELETE
```

```ruby
tax_rate.delete
```

```php
<?php

$taxRate->delete();
```

```python
tax_rate.delete()
```

```java
taxRate.delete();
```

> The above command returns `204 No Content`

This endpoint deletes a specific tax rate.

### HTTP Request

`DELETE /tax_rates/:id`

## List all tax rates

```shell
curl "https://api.invoiced.com/tax_rates" \
  -u {API_KEY}:
```

```ruby
tax_rates, metadata = invoiced.TaxRate.list(:per_page => 3)
```

```php
<?php

list($taxRate, $metadata) = $invoiced->TaxRate->all(['per_page' => 3]);
```

```python
tax_rates, metadata = invoiced.TaxRate.list(per_page=3)
```

```java
EntityList<TaxRate> taxRate = conn.newTaxRate().listAll();
```

> The above command returns JSON structured like this:

```shell
[
  <%= json data.pricing.tax_rates.object, 1 %>
]
```

```ruby
[
  #<Invoiced::TaxRate:0x3fdbf95e4d08 id=46225> JSON: <%= json data.pricing.tax_rates.object, 1 %>
]
```

```php
[
  Invoiced\TaxRate JSON: <%= json data.pricing.tax_rates.object, 1 %>
]
```

```python
[
  <TaxRate id=46225 at 0x3fdbf95e4d08> JSON: <%= json data.pricing.tax_rates.object, 1 %>
]
```

```java
[
  com.invoiced.entity.TaxRate@c509a57 JSON: <%= json data.pricing.tax_rates.object, 1 %>
]
```

This endpoint retrieves all tax rates.

### HTTP Request

`GET /tax_rates`

### Query Parameters

Parameter | Description
--------- | -----------
**sort** *string* | Column to sort by, i.e. `name asc`
**filter** *object* | Filter object
**updated_after** *timestamp* | Only gets records updated after the given timestamp

## Coupon Object

A coupon describes a discount that can be applied to [invoices](#invoice-object), [line items](#line-item-object), and [subscriptions](#subscription-object).

### Attributes

```shell
<%= json data.pricing.coupons.object %>
```

```ruby
#<Invoiced::Coupon:0x3fdbf95e4d08 id=S8L47J> JSON: <%= json data.pricing.coupons.object %>
```

```php
Invoiced\Coupon JSON: <%= json data.pricing.coupons.object %>
```

```python
<Coupon id=S8L47J at 0x3fdbf95e4d08> JSON: <%= json data.pricing.coupons.object %>
```

```java
com.invoiced.entity.Coupon@192cafae JSON: <%= json data.pricing.coupons.object %>
```

Parameter | Type | Description
--------- | ---- | -----------
**id** | *integer* | The coupon's unique ID
**object** | *string* | Object type, `coupon`
**name** | *string* | Coupon name
**currency** | *string* | [3-letter ISO code](https://en.wikipedia.org/wiki/ISO_4217)
**value** | *number* | Amount
**is_percent** | *boolean* | When true the value is a %
**exclusive** | *boolean* | When true no other coupons can be used simultaneously
**expiration_date** | *timestamp* | Date coupon expires
**max_redemptions** | *integer* | Max number of times coupon can be used
**created_at** | *timestamp* | Timestamp when created
**metadata** | *object* | A hash of key/value pairs that can store additional information about this object.