# Files

You can add external files to Invoiced through the API and attach them to invoices. The [File Attachments Guide](https://invoiced.com/resources/docs/dev/file-attachments) details the entire workflow.

## File Object

Represents an external file.

### Attributes

```shell
<%= json data.files.object %>
```

```ruby
#<Invoiced::File:0x3fdbf95e4d08 id=13> JSON: <%= json data.files.object %>
```

```php
Invoiced\File JSON: <%= json data.files.object %>
```

```python
<File id=13 at 0x3fdbf95e4d08> JSON: <%= json data.files.object %>
```

```java
com.invoiced.entity.File@192cafae JSON: <%= json data.files.object %>
```

```csharp
Invoiced.File<13> <%= json data.files.object %>
```

```go
<File id=13 at 192cafae> JSON: <%= json data.files.object %>
```

Parameter | Type | Description
--------- | ---- | -----------
**id** | *integer* | The file's unique ID
**object** | *string* | Object type, `file`
**name** | *string* | Filename
**size** | *int* | File size in bytes
**type** | *string* | MIME type
**url** | *string* | File URL
**created_at** | *timestamp* | Timestamp when created
**updated_at** | *timestamp* | Timestamp when updated

## Create a file

```shell
curl "https://api.invoiced.com/files" \
  -u {API_KEY}: \
  -d url="https://invoiced.com/img/logo-invoice.png" \
  -d name="logo-invoice.png" \
  -d size=6936 \
  -d type="image/png"
```

```ruby
invoiced.File.create(
  :url => "https://invoiced.com/img/logo-invoice.png",
  :name => "logo-invoice.png",
  :size => 6936,
  :type => "image/png"
)
```

```php
<?php

$invoiced->File->create([
  'url' => "https://invoiced.com/img/logo-invoice.png",
  'name' => "logo-invoice.png",
  'size' => 6936,
  'type' => "image/png"
]);
```

```python
client.File.create(
  url="https://invoiced.com/img/logo-invoice.png",
  name="logo-invoice.png",
  size=6936,
  type="image/png"
)
```

```java
File file = invoiced.newFile();
file.url = "https://invoiced.com/img/logo-invoice.png";
file.size = 6936;
file.name = "logo-invoice.png";
file.type = "image/png";
file.create();
```

```csharp
var file = invoiced.NewFile();
file.Url = "https://invoiced.com/img/logo-invoice.png";
file.Size = 6936;
file.Name = "logo-invoice.png";
file.Type = "image/png";
file.Create();
```

```go
file := invoiced.NewFile()
file.Url = "https://invoiced.com/img/logo-invoice.png"
file.Size = 6936
file.Name = "logo-invoice.png"
file.Type = "image/png"
file, err := file.Create(file)
```

> The above command returns JSON structured like this:

```shell
<%= json data.files.object %>
```

```ruby
#<Invoiced::File:0x3fdbf95e4d08 id=13> JSON: <%= json data.files.object %>
```

```php
Invoiced\File JSON: <%= json data.files.object %>
```

```python
<File id=13 at 0x3fdbf95e4d08> JSON: <%= json data.files.object %>
```

```java
com.invoiced.entity.File@106505f5 JSON: <%= json data.files.object %>
```

```csharp
Invoiced.File<13> <%= json data.files.object %>
```

```go
<File id=13 at 106505f5> JSON: <%= json data.files.object %>
```

Create a new file object with this endpoint.

### HTTP Request

`POST /files`

### Attributes

The file should be uploaded as [multipart/form-data](https://tools.ietf.org/html/rfc7578). The `name` parameter on the uploaded part should be `file`. Any other parts in the request will be ignored. If a part with `name=file` is not supplied then the request will fail.

## Retrieve a file

```shell
curl "https://api.invoiced.com/files/:id" \
  -u {API_KEY}:
```

```ruby
file = invoiced.File.retrieve("{FILE_ID}")
```

```php
<?php

$file = $invoiced->File->retrieve("{FILE_ID}");
```

```python
file = client.File.retrieve("{FILE_ID}")
```

```java
File file = invoiced.newFile().retrieve({FILE_ID});
```

```csharp
var file = invoice.NewFile().Retrieve({FILE_ID});
```

```go
file, err := invoice.NewFile().Retrieve({FILE_ID})
```

> The above command returns JSON structured like this:

```shell
<%= json data.files.object %>
```

```ruby
#<Invoiced::File:0x3fdbf95e4d08 id=13> JSON: <%= json data.files.object %>
```

```php
Invoiced\File JSON: <%= json data.files.object %>
```

```python
<File id=13 at 0x3fdbf95e4d08> JSON: <%= json data.files.object %>
```

```java
com.invoiced.entity.File@106505f5 JSON: <%= json data.files.object %>
```

```csharp
Invoiced.File<13> <%= json data.files.object %>
```

```go
<File id=13 at 192cafae> JSON: <%= json data.files.object %>
```

This endpoint retrieves a specific file.

### HTTP Request

`GET /files/:id`

## Delete a file

```shell
curl "https://api.invoiced.com/files/:id" \
  -u {API_KEY}: \
  -X DELETE
```

```ruby
file.delete
```

```php
<?php

$file->delete();
```

```python
file.delete()
```

```java
file.delete();
```

```csharp
file.Delete();
```

```go
err := file.Delete()
```

> The above command returns `204 No Content`

This endpoint deletes a specific file.

### HTTP Request

`DELETE /files/:id`